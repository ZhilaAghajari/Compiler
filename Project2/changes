Original one:
ClassBody:LBRACEnum Decls RBRACEnum
{
	/*$$ = MakeTree(BodyOp,$2);*/
	$$ = $2; /*return Decls as output */
}
		| LBRACEnum MethodDeclLoop RBRACEnum
{
	/*$$ = MakeTree(BodyOp,NullExp(),$2);*/
	/* Null pointer must be the child of Methoddeclloop tree, since decls(null here) is the left child of methoddecl*/
	$$ = MkLeftC(NullExp(),$2);
}
		| LBRACEnum Decls MethodDecl RBRACEnum
{
	/*$$ = MakeTree(BodyOp,$2,$3);*/
	/* Null pointer must be the child of Methoddeclloop tree, since decls(null here) is the left child of methoddecl*/
	$$ = MkLeftC($2,$3);
}
  		| LBRACEnum Decls MethodDeclLoop RBRACEnum
{
	/*$$ = MakeTree(BodyOp,$2,$3);*/
	$$ = MkLeftC($2,$3);
}
;

changed one:
